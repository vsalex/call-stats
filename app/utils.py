# ISO 8601 formatted datetime string
timestamp_format = "%Y-%m-%dT%H:%M:%S"

# Phone number pattern for re module
number_format = "^\+[0-9] \(([0-9]{3})+\) ([0-9]{7})+$"

# Phone code pattern for re module
# for example +7 (903)
code_number_format = "^\+[0-9] \(([0-9]{3})+\)"

call_id_format = str
duration_format = int
